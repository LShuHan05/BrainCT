# ====================== 医学影像处理 ======================
pydicom>=2.3.0
pylibjpeg>=2.0.0
pylibjpeg-libjpeg>=2.1.0
pylibjpeg-openjpeg>=2.0.0
SimpleITK>=2.2.0
nibabel>=5.0.0

# ====================== 数据处理（固定 NumPy 版本，避免 2.x 冲突） ======================
numpy==1.26.4
pandas>=2.0.0
scipy>=1.10.0
Pillow>=9.5.0

# ====================== Web 框架 ======================
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6       # 文件上传支持

# ====================== 数据库（PostgreSQL） ======================
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# ====================== HTTP 客户端 ======================
requests>=2.31.0
httpx>=0.25.0                 # 异步 HTTP 客户端

# ====================== 可视化、UI 与进度条 ======================
matplotlib>=3.7.0
plotly>=5.0.0                 # 交互式可视化（用于 Gradio 绘图）
tqdm>=4.65.0

# ====================== Web UI 前端（修复缺失） ======================
gradio>=4.0.0                 # 补充缺失的 Gradio UI 框架

# ====================== 推理与深度学习（CUDA 11.8 版本） ======================
# 注意：PyTorch 2.3.1 系列，CUDA 11.8 版本
# 安装命令：pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cu118
#torch==2.3.1+cu118
#torchvision==0.18.1+cu118
#torchaudio==2.3.1+cu118

# ====================== 配置与工具 ======================
pydantic>=2.0.0
python-dotenv>=1.0.0
tenacity>=8.2.0               # 重试机制
loguru>=0.7.0                 # 结构化日志

# ====================== 图像处理 ======================
opencv-python-headless>=4.8.0